function introduction(name){
    return `My name is ${name}`
}

const introductionArrow = name => `My name is ${name}`

const sum = (n1, n2) => n1 + n2

const sumSmallNumbers = (n1, n2) => {
    if(n1 > 10 || n2 > 10) {
        return "Just number between 1 to 9"
    }else {
        return num1 + num2
    }
}